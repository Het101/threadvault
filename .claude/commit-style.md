# Commit style

- Never add `Co-Authored-By: Claude` or `Co-Authored-By: Claude Code`.
- Subject: imperative, lowercase after the type (`fix(ci): require Node 22+`).
- Body only when the why is not obvious from the subject.
