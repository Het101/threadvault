node_modules/
dist/
coverage/
.env
.env.*
!.env.example
*.jsonl
# except the synthetic one CI runs a real command against. Lorem only, never
# anything resembling a chat message.
!test/fixtures/*.jsonl
acs-dump*/

# A local threadvault.yml names the table and column layout of whatever
# database it is pointed at, which for the people using this tool is a
# production schema. Only the example belongs in the repo.
threadvault.yml
threadvault.yaml
!threadvault.example.yml

.threadvault/
*.log
.DS_Store
Thumbs.db
replay*.json

# Drafts kept locally, not part of the published project
linkedin-post.txt
SETUP-CHECKLIST.md

# Coverage output, regenerated by `npm run test:coverage`
